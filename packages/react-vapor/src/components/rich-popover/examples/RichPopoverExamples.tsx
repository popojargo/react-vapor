import * as React from 'react';
import {RichPopover} from '../RichPopover';

export function RichPopoverExamples() {
    return (
        <div>
            <RichPopover id="rich-popover-example-1" />
        </div>
    );
}
